@model Everestfcsweb.Models.DiscountListData
<div class="modal-header">
    <h5 class="text-sm modal-title" id="SocialPesaModalLabel">Discount List Values</h5>
    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
        <span aria-hidden="true">&times;</span>
    </button>
</div>
<div class="modal-body">
    <div class="row">
        <div class="col-sm-4">
            <h4 class="text-sm font-weight-light"><b>Discount List: </b> @Model.DiscountListname</h4>
        </div>
        <div class="col-sm-4">
            <h4 class="text-sm font-weight-light"><b>Date Created: </b> @Model.Datecreated</h4>
        </div>
        <div class="col-sm-4">
            <h4 class="text-sm font-weight-light"><b>Created By: </b> @Model.Createdby</h4>
        </div>
    </div>
    <div class="row">
        <div class="col-sm-12 table-responsive table-responsive-sm">
            <table class="table table-bordered table-striped compact table-sm table-responsive-sm nowrap text-nowrap" id="AllsystemDataTables">
                <thead>
                    <tr>
                        <th class="font-weight-light">Product</th>
                        <th class="font-weight-light">Station</th>
                        <th class="font-weight-light">Value</th>
                        <th class="font-weight-light">Days</th>
                        <th class="font-weight-light">Start Time</th>
                        <th class="font-weight-light">End Time</th>
                        <th class="font-weight-light">Action</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model.Discountlistvalues != null)
                    {
                        foreach (var item in Model.Discountlistvalues)
                        {
                            <tr>
                                <td class="font-weight-light">@item.Productvariationname</td>
                                <td class="font-weight-light">@item.Station</td>
                                <td class="font-weight-light">@item.Discountvalue</td>
                                <td class="font-weight-light">@item.Daysapplicable</td>
                                <td class="font-weight-light">@item.Starttime</td>
                                <td class="font-weight-light">@item.Endtime</td>
                                <td class="font-weight-light">
                                    <a onclick="permanentdeletethis(event)" data-Tablename="PriceListprices" data-Columnidname="PriceListPricesId" data-Entryid="@item.LnkDiscountProductId" class="btn btn-xs btn-danger">Remove</a>
                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>